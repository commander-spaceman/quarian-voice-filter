# quarian-voice-filter

Reusable Rust workspace for Quarian-style voice filtering.

## Workspace

- `crates/quarian-voice-filter`: core library crate
- `crates/quarian-voice-filter-cli`: CLI crate for file-based processing

## Current status

The repository is scaffolded and builds, but the DSP implementation is still pending.
